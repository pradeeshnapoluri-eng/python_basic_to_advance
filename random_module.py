import random
# a = random.randint(1,7) #it gives from 1 to 7
# a = random.randrange(1,7) #it gives from 1 to 6
# a = random.random() #it gives the number between 0 to 1 in float
#to get random in between specific numbers we use uniform function.
# a = random.uniform(2,8) #to get decimals between 2 to 8

#to select the number/index randomly in a list we use choice() function.here we go:
# list = [1,3,4,2,82,24,7]
# a = random.choice(list)
# print(a)
#to shuffle the list we use shuffle function.we will not use any variable here
# list = [1,3,4,2,82,24,7]
# random.shuffle(list)
# print(list)
